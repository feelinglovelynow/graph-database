/** @type { 'str' } */
const str = 'str'

/** @type { 'num' } */
const num = 'num'

/** @type { 'uid' } */
const uid = 'uid'

/** @type { 'uids' } */
const uids = 'uids'

/** @typedef { str | num | uid | uids } enumDataTypes */

export const enumDataTypes = {
  str,
  num,
  uid,
  uids,
}
